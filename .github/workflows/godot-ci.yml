name: "godot-ci export"

on: 
  push:
    branches:
      - main

permissions:
  contents: write

env:
  GODOT_VERSION: 4.6
  EXPORT_NAME: signal-mage
  PROJECT_PATH: .

jobs:
  # export-windows:
  #   name: Windows Export
  #   runs-on: ubuntu-24.04
  #   container:
  #     image: barichello/godot-ci:4.6
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #       with:
  #         lfs: true
  #     # Добавляем библиотеки, так как Godot может упасть еще до начала сборки
  #     - name: Install Dependencies
  #       run: apt-get update && apt-get install -y libfontconfig1 libdbus-1-3
  #     - name: Setup
  #       run: |
  #         mkdir -v -p ~/.local/share/godot/export_templates/
  #         mkdir -v -p ~/.config/
  #         cp -rn /root/.config/godot ~/.config/godot
  #         cp -rn /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/
  #     - name: Windows Build
  #       run: |
  #         mkdir -v -p build/windows
  #         EXPORT_DIR="$(readlink -f build)"
  #         cd $PROJECT_PATH
  #         godot --headless --verbose --export-release "Windows Desktop" "$EXPORT_DIR/windows/$EXPORT_NAME.exe"
  #     - name: Upload Artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: windows
  #         path: build/windows

#   export-linux:
#     name: Linux Export
#     runs-on: ubuntu-24.04
#     container:
#       image: barichello/godot-ci:4.6
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#         with:
#           lfs: true

#       # ИСПРАВЛЕНИЕ: Установка библиотек для Linux
#       - name: Install Dependencies
#         run: |
#           apt-get update && apt-get install -y libfontconfig1 libdbus-1-3

#       - name: Setup
#         run: |
#           mkdir -v -p ~/.local/share/godot/export_templates/
#           # Используем cp -rn вместо mv, чтобы избежать ошибок доступа и пустых папок
#           cp -rn /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/
          
#       - name: Linux Build
#         run: |
#           mkdir -v -p build/linux
#           EXPORT_DIR="$(readlink -f build)"
#           cd ${PROJECT_PATH:-.} 
#           godot --headless --verbose --export-release "Linux/X11" "$EXPORT_DIR/linux/$EXPORT_NAME.x86_64"

#       - name: Upload Artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: linux
#           path: build/linux

  export-web:
    name: Web Export
    runs-on: ubuntu-24.04
    container:
      image: barichello/godot-ci:4.6
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install Dependencies
        run: apt-get update && apt-get install -y libfontconfig1 libdbus-1-3
      - name: Setup
        run: |
          mkdir -v -p ~/.local/share/godot/export_templates/
          cp -rn /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/
      - name: Web Build
        run: |
          mkdir -v -p build/web
          EXPORT_DIR="$(readlink -f build)"
          cd $PROJECT_PATH
          godot --headless --verbose --export-release "Web" "$EXPORT_DIR/web/index.html"
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: web
          path: build/web
      - name: Install rsync 📚
        run: |
          apt-get update && apt-get install -y rsync
      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@releases/v4
        with:
          branch: gh-pages
          folder: build/web